# React Redux Quote Maker Application
#### You could create quotes with author
#### Up vote or Down vote the quotes
#### Delete the quotes


### Please follow the steps: 
 `git clone project ` 
 `cd project_folder` 
 `npm install && npm start` 
